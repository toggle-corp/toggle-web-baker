---
# Source: toggle-web-baker-helm/templates/operator-rbac.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: release-name-toggle-web-baker-operator
  namespace: default
  labels:
    helm.sh/chart: toggle-web-baker-helm-0.0.0
    app.kubernetes.io/name: toggle-web-baker
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/version: "0.0.0"
    app.kubernetes.io/managed-by: Helm
---
# Source: toggle-web-baker-helm/templates/crd.yaml
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
    # helm.sh/resource-policy keeps the CRD on `helm uninstall` so existing
    # FrontendApp CRs are not cascade-deleted.
    helm.sh/resource-policy: keep
  name: frontendapps.baker.toggle-corp.com
spec:
  group: baker.toggle-corp.com
  names:
    kind: FrontendApp
    listKind: FrontendAppList
    plural: frontendapps
    shortNames:
    - fapp
    singular: frontendapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .status.specStale
      name: Stale
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FrontendApp is the Schema for the frontendapps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: 'FrontendAppSpec is the desired state: operational tunables
              for one app.'
            properties:
              auth:
                description: |-
                  AuthConfig configures optional HTTP basic auth. Exactly one of passwordHash or
                  secretRef must be set.
                properties:
                  passwordHash:
                    type: string
                  secretRef:
                    description: AuthSecretRef points at a Secret key holding a bcrypt/htpasswd
                      line.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
                x-kubernetes-validations:
                - message: auth requires exactly one of passwordHash or secretRef
                  rule: '(has(self.passwordHash) ? 1 : 0) + (has(self.secretRef) ?
                    1 : 0) == 1'
              build:
                description: |-
                  PhaseSpec describes one pipeline phase container (setup / fetch / build). The
                  public-env / no-secretKeyRef boundary holds STRUCTURALLY: EnvVarSource has no
                  secretKeyRef field, so a CEL rule would be tautological — none is declared.
                properties:
                  command:
                    items:
                      type: string
                    type: array
                  env:
                    items:
                      description: |-
                        EnvVar is a PUBLIC environment variable (literal or ConfigMap-sourced). It can
                        reach the built bundle, so it must never carry secrets.
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          description: |-
                            EnvVarSource is the inline (NO $ref) source for a public EnvVar. Only a
                            ConfigMap key reference is permitted; secretKeyRef is intentionally absent so
                            secrets can never leak into setup/build env via this type.
                          properties:
                            configMapKeyRef:
                              description: ConfigMapKeySelector selects a key from
                                a ConfigMap in the app namespace.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  image:
                    type: string
                  runAsUser:
                    description: |-
                      RunAsUser pins this phase container's numeric UID. The build pod sets
                      runAsNonRoot WITHOUT a UID, so an image whose USER is a non-numeric name
                      (e.g. cimg/node's `circleci`) is rejected at admission — the kubelet
                      cannot verify a named user is non-root (CreateContainerConfigError). Set
                      this to the image's numeric non-root UID to satisfy the constraint. Must
                      be > 0 (non-root).
                    format: int64
                    minimum: 1
                    type: integer
                type: object
              buildArgs:
                description: BuildArgs are PUBLIC, ConfigMap-sourced env that may
                  reach the bundle.
                items:
                  description: |-
                    EnvVar is a PUBLIC environment variable (literal or ConfigMap-sourced). It can
                    reach the built bundle, so it must never carry secrets.
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                    valueFrom:
                      description: |-
                        EnvVarSource is the inline (NO $ref) source for a public EnvVar. Only a
                        ConfigMap key reference is permitted; secretKeyRef is intentionally absent so
                        secrets can never leak into setup/build env via this type.
                      properties:
                        configMapKeyRef:
                          description: ConfigMapKeySelector selects a key from a ConfigMap
                            in the app namespace.
                          properties:
                            key:
                              type: string
                            name:
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              fetch:
                description: |-
                  PhaseSpec describes one pipeline phase container (setup / fetch / build). The
                  public-env / no-secretKeyRef boundary holds STRUCTURALLY: EnvVarSource has no
                  secretKeyRef field, so a CEL rule would be tautological — none is declared.
                properties:
                  command:
                    items:
                      type: string
                    type: array
                  env:
                    items:
                      description: |-
                        EnvVar is a PUBLIC environment variable (literal or ConfigMap-sourced). It can
                        reach the built bundle, so it must never carry secrets.
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          description: |-
                            EnvVarSource is the inline (NO $ref) source for a public EnvVar. Only a
                            ConfigMap key reference is permitted; secretKeyRef is intentionally absent so
                            secrets can never leak into setup/build env via this type.
                          properties:
                            configMapKeyRef:
                              description: ConfigMapKeySelector selects a key from
                                a ConfigMap in the app namespace.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  image:
                    type: string
                  runAsUser:
                    description: |-
                      RunAsUser pins this phase container's numeric UID. The build pod sets
                      runAsNonRoot WITHOUT a UID, so an image whose USER is a non-numeric name
                      (e.g. cimg/node's `circleci`) is rejected at admission — the kubelet
                      cannot verify a named user is non-root (CreateContainerConfigError). Set
                      this to the image's numeric non-root UID to satisfy the constraint. Must
                      be > 0 (non-root).
                    format: int64
                    minimum: 1
                    type: integer
                type: object
              ingress:
                description: IngressConfig describes the public ingress for the served
                  bundle.
                properties:
                  className:
                    type: string
                  host:
                    type: string
                  tls:
                    description: TLSConfig configures TLS termination at the Ingress.
                    properties:
                      secretName:
                        type: string
                    required:
                    - secretName
                    type: object
                required:
                - host
                type: object
              keepReleases:
                default: 0
                type: integer
              outputDir:
                type: string
              packageManager:
                default: yarn
                description: PackageManager selects the JS package manager (drives
                  the volume layout).
                enum:
                - yarn
                - pnpm
                type: string
              ref:
                default: HEAD
                type: string
              repo:
                type: string
              resources:
                description: ResourcesConfig groups resource and deadline tunables.
                properties:
                  activeDeadlineSeconds:
                    default: 1800
                    format: int64
                    type: integer
                  build:
                    description: BuildResources configures the build container's resource
                      constraints.
                    properties:
                      cpuRequest:
                        type: string
                      memoryLimit:
                        default: 6Gi
                        type: string
                      memoryRequest:
                        type: string
                    type: object
                type: object
              schedule:
                default: 0 */12 * * *
                type: string
              secrets:
                description: Secrets are Secret-sourced env injected into the FETCH
                  phase ONLY.
                items:
                  description: |-
                    EnvVarWithSecret is a Secret-sourced environment variable, intended for the
                    FETCH phase ONLY. The operator injects these solely into the fetch container.
                  properties:
                    name:
                      type: string
                    valueFrom:
                      description: EnvVarWithSecretSource is the inline source for
                        a secret-backed env var.
                      properties:
                        secretKeyRef:
                          description: SecretKeySelector selects a key from a Secret
                            in the app namespace.
                          properties:
                            key:
                              type: string
                            name:
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - secretKeyRef
                      type: object
                  required:
                  - name
                  - valueFrom
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              setup:
                description: |-
                  PhaseSpec describes one pipeline phase container (setup / fetch / build). The
                  public-env / no-secretKeyRef boundary holds STRUCTURALLY: EnvVarSource has no
                  secretKeyRef field, so a CEL rule would be tautological — none is declared.
                properties:
                  command:
                    items:
                      type: string
                    type: array
                  env:
                    items:
                      description: |-
                        EnvVar is a PUBLIC environment variable (literal or ConfigMap-sourced). It can
                        reach the built bundle, so it must never carry secrets.
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          description: |-
                            EnvVarSource is the inline (NO $ref) source for a public EnvVar. Only a
                            ConfigMap key reference is permitted; secretKeyRef is intentionally absent so
                            secrets can never leak into setup/build env via this type.
                          properties:
                            configMapKeyRef:
                              description: ConfigMapKeySelector selects a key from
                                a ConfigMap in the app namespace.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  image:
                    type: string
                  runAsUser:
                    description: |-
                      RunAsUser pins this phase container's numeric UID. The build pod sets
                      runAsNonRoot WITHOUT a UID, so an image whose USER is a non-numeric name
                      (e.g. cimg/node's `circleci`) is rejected at admission — the kubelet
                      cannot verify a named user is non-root (CreateContainerConfigError). Set
                      this to the image's numeric non-root UID to satisfy the constraint. Must
                      be > 0 (non-root).
                    format: int64
                    minimum: 1
                    type: integer
                type: object
              storage:
                description: |-
                  StorageConfig groups the per-volume absolute-byte thresholds. The operator
                  also calls domain.ValidateStorage at reconcile time (cleanup < alert < cap).
                properties:
                  cache:
                    description: CacheThresholds are absolute-byte thresholds for
                      the regenerable cache volume.
                    properties:
                      alertBytes:
                        format: int64
                        type: integer
                      cleanupBytes:
                        format: int64
                        type: integer
                    type: object
                  dataCache:
                    description: DataCacheThresholds adds a per-run delta budget on
                      top of cache thresholds.
                    properties:
                      alertBytes:
                        format: int64
                        type: integer
                      cleanupBytes:
                        format: int64
                        type: integer
                      runDeltaBytes:
                        format: int64
                        type: integer
                    type: object
                  node:
                    description: NodeStorage describes node-level headroom expectations.
                    properties:
                      freeSpaceHeadroomBytes:
                        format: int64
                        type: integer
                    type: object
                  output:
                    description: OutputThresholds bound the served-bundle volume.
                    properties:
                      alertBytes:
                        format: int64
                        type: integer
                      capBytes:
                        format: int64
                        type: integer
                    type: object
                type: object
                x-kubernetes-validations:
                - message: cache.cleanupBytes must be < cache.alertBytes
                  rule: '!has(self.cache) || !has(self.cache.cleanupBytes) || !has(self.cache.alertBytes)
                    || self.cache.cleanupBytes < self.cache.alertBytes'
                - message: dataCache.cleanupBytes must be < dataCache.alertBytes
                  rule: '!has(self.dataCache) || !has(self.dataCache.cleanupBytes)
                    || !has(self.dataCache.alertBytes) || self.dataCache.cleanupBytes
                    < self.dataCache.alertBytes'
                - message: output.alertBytes must be < output.capBytes
                  rule: '!has(self.output) || !has(self.output.alertBytes) || !has(self.output.capBytes)
                    || self.output.alertBytes < self.output.capBytes'
              submodules:
                type: boolean
            required:
            - ingress
            - repo
            type: object
            x-kubernetes-validations:
            - message: build.command is required
              rule: has(self.build) && has(self.build.command) && size(self.build.command)
                > 0
            - message: secrets require a fetch.command to consume them
              rule: '!has(self.secrets) || size(self.secrets) == 0 || (has(self.fetch)
                && has(self.fetch.command) && size(self.fetch.command) > 0)'
          status:
            description: FrontendAppStatus is the operator-owned observed state.
            properties:
              build:
                description: BuildStatus mirrors the current/last build Job.
                properties:
                  attempts:
                    type: integer
                  completionTime:
                    format: date-time
                    type: string
                  jobName:
                    type: string
                  logsRef:
                    type: string
                  message:
                    type: string
                  phase:
                    description: BuildPhase is the lifecycle of the current/last build
                      pod.
                    type: string
                  result:
                    description: BuildResult is the terminal outcome of a build pod.
                    type: string
                  startTime:
                    format: date-time
                    type: string
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dataFreshness:
                type: string
              lastBuildTime:
                format: date-time
                type: string
              lastBuiltSpecHash:
                type: string
              lastManualTrigger:
                description: ManualTrigger records the last manual rebuild request.
                properties:
                  time:
                    format: date-time
                    type: string
                  triggeredBy:
                    type: string
                type: object
              lastProcessedRebuild:
                type: string
              lastSuccessfulBuildTime:
                format: date-time
                type: string
              nextScheduledBuildTime:
                format: date-time
                type: string
              nodeName:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: Phase is the derived top-level lifecycle phase (computed
                  from conditions).
                type: string
              release:
                description: ReleaseStatus tracks the served release pointers.
                properties:
                  current:
                    type: string
                  previous:
                    type: string
                  servingSince:
                    format: date-time
                    type: string
                type: object
              specStale:
                type: boolean
              storage:
                description: StorageStatus records the most recent du measurement.
                properties:
                  lastRunDeltas:
                    additionalProperties:
                      format: int64
                      type: integer
                    type: object
                  measuredAt:
                    format: date-time
                    type: string
                  sizes:
                    additionalProperties:
                      format: int64
                      type: integer
                    type: object
                  thresholdState:
                    type: string
                type: object
              url:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
# Source: toggle-web-baker-helm/templates/operator-rbac.yaml
# Operator ClusterRole — kept in sync with config/rbac/role.yaml
# (regenerate that file with `just manifests`, then mirror changes here).
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: release-name-toggle-web-baker-operator
  labels:
    helm.sh/chart: toggle-web-baker-helm-0.0.0
    app.kubernetes.io/name: toggle-web-baker
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/version: "0.0.0"
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  - persistentvolumeclaims
  - serviceaccounts
  - services
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - persistentvolumes
  - secrets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - delete
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - pods/log
  verbs:
  - get
- apiGroups:
  - apps
  resources:
  - deployments
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - baker.toggle-corp.com
  resources:
  - frontendapps
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - baker.toggle-corp.com
  resources:
  - frontendapps/finalizers
  verbs:
  - update
- apiGroups:
  - baker.toggle-corp.com
  resources:
  - frontendapps/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - batch
  resources:
  - cronjobs
  - jobs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - networking.k8s.io
  resources:
  - ingresses
  - networkpolicies
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - rolebindings
  - roles
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - storage.k8s.io
  resources:
  - storageclasses
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - traefik.io
  resources:
  - middlewares
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
---
# Source: toggle-web-baker-helm/templates/operator-rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: release-name-toggle-web-baker-operator
  labels:
    helm.sh/chart: toggle-web-baker-helm-0.0.0
    app.kubernetes.io/name: toggle-web-baker
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/version: "0.0.0"
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: release-name-toggle-web-baker-operator
subjects:
  - kind: ServiceAccount
    name: release-name-toggle-web-baker-operator
    namespace: default

---
# Source: toggle-web-baker-helm/templates/operator-rbac.yaml
# Leader-election Role — lease + event access, scoped to the operator's own namespace.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: release-name-toggle-web-baker-operator-leader-election
  namespace: default
  labels:
    helm.sh/chart: toggle-web-baker-helm-0.0.0
    app.kubernetes.io/name: toggle-web-baker
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/version: "0.0.0"
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - create
  - get
  - list
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
# Source: toggle-web-baker-helm/templates/operator-rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: release-name-toggle-web-baker-operator-leader-election
  namespace: default
  labels:
    helm.sh/chart: toggle-web-baker-helm-0.0.0
    app.kubernetes.io/name: toggle-web-baker
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/version: "0.0.0"
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: release-name-toggle-web-baker-operator-leader-election
subjects:
  - kind: ServiceAccount
    name: release-name-toggle-web-baker-operator
    namespace: default
---
# Source: toggle-web-baker-helm/templates/operator-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: release-name-toggle-web-baker-operator
  namespace: default
  labels:
    helm.sh/chart: toggle-web-baker-helm-0.0.0
    app.kubernetes.io/name: toggle-web-baker
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/version: "0.0.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: toggle-web-baker
      app.kubernetes.io/instance: release-name
      app.kubernetes.io/component: operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: toggle-web-baker
        app.kubernetes.io/instance: release-name
        app.kubernetes.io/component: operator
    spec:
      serviceAccountName: release-name-toggle-web-baker-operator
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: ghcr.io/toggle-corp/toggle-web-baker-operator:0.0.0
          imagePullPolicy: IfNotPresent
          args:
            - --leader-elect=true
            - --metrics-bind-address=:8080
            - --health-probe-bind-address=:8081
            - --cluster-cidrs=10.0.0.0/8
            - --cluster-cidrs=172.20.0.0/16
            - --registry-allowlist=ghcr.io/toggle-corp/
            - --traefik-namespace=traefik
            - --traefik-group=traefik.io
            - --image-clone=ghcr.io/toggle-corp/toggle-web-baker-clone:0.0.0
            - --image-copier=ghcr.io/toggle-corp/toggle-web-baker-copier:0.0.0
            - --image-du=ghcr.io/toggle-corp/toggle-web-baker-du:0.0.0
            - --image-cleanup=ghcr.io/toggle-corp/toggle-web-baker-cleanup:0.0.0
            - --image-kubectl=registry.k8s.io/kubectl:v1.32.1
            - --image-nginx=docker.io/nginxinc/nginx-unprivileged:1.27-alpine
          ports:
            - name: metrics
              containerPort: 8080
            - name: health
              containerPort: 8081
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 10m
              memory: 64Mi
